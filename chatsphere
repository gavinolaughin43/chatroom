<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <title>ChatSphere</title> <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-app.js"></script> <script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore.js"></script> <style> body { font-family: Arial, sans-serif; background-color: black; /* Black background */ background-image: radial-gradient(pink 10%, transparent 10%); background-size: 40px 40px; margin: 0; padding: 20px; color: white; /* White text */ } #chat-container { width: 60%; margin: 20px auto; } #chat { border: 1px solid #fff; /* White border */ padding: 10px; height: 300px; overflow-y: auto; background: #333; /* Dark grey background for chat */ text-align: left; /* Align text to the left */ } p { margin: 5px; } input { width: 70%; padding: 10px; background: #444; /* Dark background for input */ color: white; /* White text in input */ border: none; border-radius: 5px; } button { padding: 10px; background: #444; /* Dark background for button */ color: white; /* White text on button */ border: none; border-radius: 5px; cursor: pointer; } button:hover { background: #555; /* Slightly lighter on hover */ } /* ChatGPT Credit Message */ #chatgpt-credit { position: fixed; bottom: 10px; left: 10px; font-size: 12px; color: white; background: rgba(0, 0, 0, 0.7); padding: 5px; border-radius: 5px; } </style> </head> <body> <h1 style="text-align: center;">ChatSphere</h1> <div id="chat-container"> <div id="chat"></div> <input id="message" type="text" placeholder="Type a message..."> <button id="sendButton">Send</button> </div> <!-- Credit Message --> <div id="chatgpt-credit">Thanks to ChatGPT for enabling me to make this</div> <script> // Firebase configuration (replace with your own Firebase config) const firebaseConfig = { apiKey: "YOUR_API_KEY", authDomain: "YOUR_PROJECT_ID.firebaseapp.com", projectId: "YOUR_PROJECT_ID", storageBucket: "YOUR_PROJECT_ID.appspot.com", messagingSenderId: "YOUR_MESSAGING_SENDER_ID", appId: "YOUR_APP_ID" }; // Initialize Firebase firebase.initializeApp(firebaseConfig); const db = firebase.firestore(); // Send message function function sendMessage() { let msg = document.getElementById("message").value; if (msg.trim() !== "") { db.collection("messages").add({ text: msg, timestamp: firebase.firestore.FieldValue.serverTimestamp() }); document.getElementById("message").value = ""; // Clear the input after sending } } // Real-time listener for new messages db.collection("messages").orderBy("timestamp").onSnapshot(snapshot => { let chat = document.getElementById("chat"); chat.innerHTML = ""; snapshot.forEach(doc => { chat.innerHTML += "<p>" + doc.data().text + "</p>"; }); chat.scrollTop = chat.scrollHeight; // Auto scroll to the bottom }); // Attach event listener to the send button document.getElementById("sendButton").addEventListener("click", sendMessage); // Optional: Allow 'Enter' key to send message as well document.getElementById("message").addEventListener("keypress", function(event) { if (event.key === "Enter") { sendMessage(); } }); </script> </body> </html>
